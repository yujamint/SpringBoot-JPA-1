spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
##        show-sql: true # SQL을 System.out을 통해 생성한다.
#        format-sql: true
logging:
  level:
    org.hibernate.SQL: debug  # hibernate가 생성하는 모든 SQL을 로그를 통해 생성한다.

